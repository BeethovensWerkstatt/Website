services:
  website:
    build:
      context: ../api
      dockerfile: ../api/Dockerfile
    ports:
      - "4000:4000"
  api:
    build:
      context: .
      dockerfile: Dockerfile.static-nginx
    ports:
      - "8080:8080"
